#!/usr/bin/env zsh

source ${MDX_TMUX_DIR}/sessions/lib/helper.zsh

setup <SessionName>

root="${HOME}/.dotfiles"

new_session <FirstWindow> "${root}" <command>
title_pane 1 <Pane Title>

new_window <SecondWindow> "${root}" <command>
title_pane 1 <Pane Title>

clean_up
