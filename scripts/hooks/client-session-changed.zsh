#!/usr/bin/env zsh
set -euo pipefail

source "${MDX_TMUX_DIR}/scripts/lib/utils.zsh"

"${scripts_dir}"/load-session-theme.zsh
# "${scripts_dir}"/display-session-tip.zsh
# "${scripts_dir}"/display-pane-tip.zsh
